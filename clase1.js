var nombre = 'Vince', apellido = 'Restrepo'

var nombreMayusculas = nombre.toUpperCase()

var primeraLetraDeNombre = nombre.charAt(0)

var nombreCompleto = `${nombre} ${apellido.toUpperCase()}`

var str = nombre.substr(1, 3)
